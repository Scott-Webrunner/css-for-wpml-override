# css-for-wpml-override
CSS code for overriding language switcher for the WPML plugin

This CSS is to overide or wipe all style on the WPML custom lang switcher.

--> do_action(‘wpml_add_language_selector’);
